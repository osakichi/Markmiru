# ビルド成果物（配布物・実行バイナリ）。コミットしない。
build/bin
build/README.md
/dist

# フロントエンドは撤去済み（Go-SSR）。`wails build` が bindings を再生成する
# frontend/wailsjs だけが残るため、frontend/ 全体を無視する。
frontend/

# 念のため（フォント再生成ツールが生成し得るディレクトリ。通常ビルドでは生じない）。
node_modules
